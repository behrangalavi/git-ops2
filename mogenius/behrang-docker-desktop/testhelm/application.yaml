apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podinfo-1
  namespace: mogenius-argo-cd
  labels:
    managed-by: mogenius
spec:
  project: mogenius
  sources:
    # 🟩 Quelle 1: das offizielle Helm-Chart
    - repoURL: https://stefanprodan.github.io/podinfo
      chart: podinfo
      targetRevision: 6.9.2
      helm:
        valueFiles:
          - $values/mogenius/behrang-docker-desktop/testhelm/values.yaml   # 🔹 hier liegt deine Datei im Git

    # 🟩 Quelle 2: dein eigenes Git-Repo mit values.yaml
    - repoURL: https://github.com/behrangalavi/git-ops2.git   # 🔹 dein GitHub Repo!
      targetRevision: main
      ref: values                                              # Referenz für $values
  destination:
    name: in-cluster
    namespace: podinfo-1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
